package: "supertest"
description: "Node.js package - supertest"
semver: ">=1.0.0"

postconditions:
  - id: supertest-001
    summary: "Operations must handle errors appropriately"
    severity: ERROR
    behavior: |
      All async operations must be wrapped in try-catch blocks.
    patterns:
      - "supertest()"

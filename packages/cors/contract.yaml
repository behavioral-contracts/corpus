package: "cors"
description: "Node.js package - cors"
semver: ">=1.0.0"

postconditions:
  - id: cors-001
    summary: "Package operations should handle errors appropriately"
    severity: ERROR
    behavior: |
      Operations must handle potential errors.
    patterns:
      - "cors()"

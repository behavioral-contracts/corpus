package: "express-validator"
status: in-development
description: "Node.js package - express-validator"
semver: ">=1.0.0"

postconditions:
  - id: express-validator-001
    summary: "Package operations should handle errors appropriately"
    severity: ERROR
    behavior: |
      Operations must handle potential errors.
    patterns:
      - "express-validator()"

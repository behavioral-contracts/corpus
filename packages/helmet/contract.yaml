package: "helmet"
description: "Node.js package - helmet"
semver: ">=1.0.0"

postconditions:
  - id: helmet-001
    summary: "Package operations should handle errors appropriately"
    severity: ERROR
    behavior: |
      Operations must handle potential errors.
    patterns:
      - "helmet()"

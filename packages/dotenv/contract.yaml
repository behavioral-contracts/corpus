package: "dotenv"
description: "Node.js package - dotenv"
semver: ">=1.0.0"

postconditions:
  - id: dotenv-001
    summary: "Package operations should handle errors appropriately"
    severity: ERROR
    behavior: |
      Operations must handle potential errors.
    patterns:
      - "dotenv()"

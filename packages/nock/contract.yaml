package: "nock"
description: "Node.js package - nock"
semver: ">=1.0.0"

postconditions:
  - id: nock-001
    summary: "Operations must handle errors appropriately"
    severity: ERROR
    behavior: |
      All async operations must be wrapped in try-catch blocks.
    patterns:
      - "nock()"

package: "axios-retry"
status: in-development
description: "Node.js package - axios-retry"
semver: ">=1.0.0"

postconditions:
  - id: axios_retry-001
    summary: "Operations must handle errors appropriately"
    severity: ERROR
    behavior: |
      All async operations must be wrapped in try-catch blocks.
    patterns:
      - "axios-retry()"

package: "morgan"
description: "Node.js package - morgan"
semver: ">=1.0.0"

postconditions:
  - id: morgan-001
    summary: "Package operations should handle errors appropriately"
    severity: ERROR
    behavior: |
      Operations must handle potential errors.
    patterns:
      - "morgan()"
